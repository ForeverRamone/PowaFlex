# Contexto de build mínimo: todo lo que se instala viene del lock, no del host.
**/node_modules
/data
.git
.github
server/test
web/dist
**/*.db
**/*.db-wal
**/*.db-shm
.DS_Store
docs
assets

# la base y la caché de carteles del desarrollo: nunca al contexto de build
server/data/
